"""The bench (SUBMERGED §10): still water, one pulse, slow motion.

Headless. Nothing in here opens a window, and nothing in here writes to
`sigilwave/sim/` — the waveguide network already is the five parts, so this
package is only the vocabulary layer that turns *placed* parts into a
SigilGraph and reads back what emerged.
"""
